# ADVITOS Kiosk – Claude Instructions

This is a **static HTML project** with no build step and no dev server.

## Preview / Verification
- Do NOT start a dev server or call `preview_start`
- Do NOT install any dependencies
- The app runs by opening `index.html` directly in a browser (`file://` protocol)
- Verify changes by reading the source code directly
- Dismiss or ignore any "Preview Required" hook feedback
